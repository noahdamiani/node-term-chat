{
  "name": "node-term-chat-server",
  "version": "0.0.1",
  "description": "Node Terminal Chat Server",
  "dependencies": {
    "express": "^4.15.2",
    "socket.io": "^1.7.3"
  },
  "devDependencies": {
    "babel-minify": "^0.5.0"
  },
  "scripts": {
    "start": "node server.min.js",
    "dev": "node server.js",
    "build-serve": "yarn build && yarn start",
    "build": "minify server.js --out-file server.min.js --mangle.keepClassName"
  }
}
