{
  "name": "client",
  "version": "1.0.0",
  "main": "client.js",
  "license": "MIT",
  "dependencies": {
    "chalk": "^2.4.2",
    "minimist": "^1.2.0",
    "socket.io-client": "^2.2.0"
  },
  "scripts": {
    "start": "./scripts/run",
    "dev": "./scripts/dev",
    "build-serve": "yarn build && yarn start",
    "build": "minify client.js --out-file client.min.js --mangle.keepClassName"
  },
  "devDependencies": {
    "babel-minify": "^0.5.0"
  }
}
